#Used from https://github.com/badsectorlabs/ludus_adcs/blob/main/tasks/main.yml
- name: Check if ADCS is already configured
  ansible.windows.win_powershell:
    script: |
      try {
        $adcsConfig = Get-CACrlDistributionPoint -ErrorAction Stop
        if ($adcsConfig) {
          $Ansible.Result = @{
            installed = $true
          }
        } else {
          $Ansible.Result = @{
            installed = $false
          }
        }
      } catch {
        $Ansible.Result = @{
          installed = $false
        }
      }
  register: adcs_status
  
- name: Install ADCS
  ansible.windows.win_feature:
    name: AD-Certificate
    state: present
    include_sub_features: true
    include_management_tools: true
  register: adcs_feature
  when: not adcs_status.result.installed

- name: Install-ADCSCertificationAuthority-PS
  ansible.windows.win_powershell:
    script: |
      [CmdletBinding()]
      param (
          [String]
          $domain_username,

          [String]
          $domain_password,

          [String]
          $ca_common_name
      )
      $pass = ConvertTo-SecureString $domain_password -AsPlainText -Force
      $Cred = New-Object System.Management.Automation.PSCredential ($domain_username, $pass)
      try {
        Install-AdcsCertificationAuthority -Credential $Cred -CAType EnterpriseRootCA -CryptoProviderName "RSA#Microsoft Software Key Storage Provider" -KeyLength 2048 -HashAlgorithmName SHA256 -ValidityPeriod Years -ValidityPeriodUnits 5 -CACommonName $ca_common_name -Force
        $Ansible.Changed = $true
      } catch {
        $Ansible.Changed = $false
      }
    error_action: stop
    parameters:
      domain_username: '{{ ludus_domain_netbios_name }}\{{ defaults.ad_domain_admin }}'
      domain_password: '{{ defaults.ad_domain_admin_password }}'
      ca_common_name: '{{ ludus_domain_netbios_name }}-CA'
  register: adcs_install
  when: not adcs_status.result.installed
  
- name: Reboot after ADCS Installation
  ansible.windows.win_reboot:
  when: adcs_feature.changed or (adcs_install.changed | default(false))

# Configure the Web Enrollment role service so /certsrv/ actually
# responds. Install-AdcsCertificationAuthority stands up the CA itself
# but does not touch the IIS side; without this task the READMEs claim
# of TAKEOVER-3 / ESC8 vulnerability isn't real (the endpoint 404s).
# The AD-Certificate feature with include_sub_features: true above
# already ships the binaries, so this is just the configuration step.
- name: Configure AD CS Web Enrollment (enables ESC8 / TAKEOVER-3 surface)
  ansible.windows.win_powershell:
    script: |
      try {
        Install-AdcsWebEnrollment -Force -ErrorAction Stop
        $Ansible.Changed = $true
      } catch {
        $Ansible.Changed = $false
      }